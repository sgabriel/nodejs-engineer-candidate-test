/**
 * Load data.json and return it's parsed contents
 * @returns Parsed JSON
 */
function loadData() {
}


/**
 * Count all flights departing airport FRA
 * @returns {number} number of flights departing FRA
 */
function countFlightsDepartingFRA() {
}

/**
 * Count all flights which are departed but not yet arrived, e.g. all flights currently enroute.
 * @returns {number} number of flights enroute
 */
function countDepartedNotArrived() {
}

/**
 * Count all booked economy passengers.
 * @returns {number} number of booked economy passengers
 */
function countBookedPassengers() {
}

/**
 * Return an array containing all airports which are part of the network
 * @returns {string[]} unique list of airport codes
 */
function allAirports() {
}

module.exports = { countFlightsDepartingFRA, countDepartedNotArrived, countBookedPassengers, allAirports };
